# AI 筛股器空壳实施计划

基线：codex/phase-5a0 / 14c3cae。仅在当前独立 worktree 实施，停在用户审阅点。

## 边界

独占 `.ai/tools/stock-screener/`、`docs/.vuepress/components/tools/stock-screener/`、`docs/tools/stock-screener.md`、`docs/zh/tools/stock-screener.md`。不改主页、导航、全局样式、其他工具、依赖锁文件或公共测试脚本。无需共享补丁；若发现需要，先写报告。无提交、推送、合并、部署。

不实现筛选、解析、后端、模型、行情、数据库；不选择市场、条件指标或数据源。不展示证券、价格、命中数、假加载或假 AI 回答。

## 页面结构

- 独立暖白画布；原站品牌与对应语言链接。
- 标题、用途和“规划中 / In planning”状态。
- 左侧意图草稿文本域：有标签与本地临时输入说明；无提交按钮。
- 左侧筛选条件占位：不生成条件、不预设指标。
- 右侧结果空状态：没有运行筛选，数据未连接。
- 下方后续能力方向：明确待设计与实现，不承诺具体指标或交付日期。
- 移动单列；键盘焦点、标签关联、两语言路由、noindex。

## 验证

先使用现有锁文件安装。查阅 VuePress 官方 frontmatter 与 Markdown SFC 文档，结合安装版本源码确认隔离渲染方式。执行 `npm run verify`，专属浏览器检查覆盖中英桌面/移动截图、320px 溢出、键盘、输入不触发请求、语言切换、深色偏好。截图和实测证据放在本目录，交接报告记录限制和未来待明确问题。
