# A v2 隔离原型技术审阅

2026-09-05。用户确认维持当前视觉与使用感受，询问技术审阅通过后是否可以提交。本轮检查当前 worktree 相对设计基线 `14c3cae6093196cc6ef7d4c1583110da30e3da58` 的原型代码、配置、测试与验收证据，没有执行 git commit、push 或部署。

## 结论

未发现阻止将当前成果提交为“隔离主页原型”的已确认缺陷。此结论不等于正式主页已完成发布验收；`/`、`/zh/` 替换、真实 AI 与部署继续属于后续独立范围。本轮没有修改已确认的视觉或交互设计。

## 检查范围

- `docs/.vuepress/prototype/loan.mjs`：输入域、零利率、稳定年金公式、月度余额更新、最后一期清偿、显示舍入边界。
- `HomepagePrototype.vue`：数值与解释快照分离、非法输入保留最后有效结果、过期引用禁用、定时任务取消/关闭/locale 变化/卸载清理，以及无 JS 降级。
- 页面与布局：两个独立 noindex 路由、异步布局、构建时版权年份、双语路径、具名 main/h1、原生控件与表格、scoped 样式。
- 变更边界：正式主页、文章与旧资产源码未修改；无新增金融/模型请求、eval、v-html、API key 或依赖升级。
- 测试：检查独立 loan oracle、浏览器状态与 SPA 回归、生产产物/noindex 检查，以及报告和实际证据的一致性。

## 本轮补充验证

`node scripts/check-prototype-loan.mjs` 再次通过；`git diff --check` 通过。

另以固定种子 20260905 运行 1,000 组场景：本金在允许范围内精确到分，年限 1–40，利率 0–20%（每十组强制一个零利率）。逐行确认有限数值、本金为正、利息/余额非负、期初余额计息关系、末期零余额；检查本金守恒、等额本金总利息不高于等额本息，以及等额本息末期与首期差额。

- 最大本金累计误差：`2.421438694000244e-8 USD`。
- 最大等额本息末期差额：`0.000005571433575823903 USD`。
- 两者均远低于 0.01 USD 展示精度。

本轮未改变应用源码，因此沿用同一版本已完成的完整 `npm run verify` 与双语 Chromium 证据，没有无必要地重新构建和重复整个截图矩阵。证据见 [verify.log](./prototype-evidence/verify.log)、[browser.log](./prototype-evidence/browser.log)、[checks.json](./prototype-evidence/checks.json)。

## 保留的非阻塞事项

- Firefox/Safari、实际触屏和人工读屏尚未验证；200% 为重排等效检查。需要在正式替换/上线验收时补齐，不能据此声称跨浏览器全面通过。
- 总初始 JS 和公共 CSS 略超候选预算，原因与共享旧主题成本已在 [原型报告](./homepage-prototype-report.md) 中透明记录；提交原型无需顺带重构全站打包。
- mock 输入仅接受三个预设问题，状态选择器仅供审阅；不能以此冒充真实 AI。正式版产品取舍另行落实。
- 新贷款与原型浏览器检查目前是独立命令，尚未并入 `npm run verify` 的日常 CI 链。原型提交可保留这个边界；正式实现应将必要断言纳入 CI，并将截图/性能采样与常规断言分离。

建议提交范围：原型代码、两个预览页面、必要配置/产物检查、新验证脚本与 `.ai/phase5` 报告/截图。建议标题：`feat: add isolated bilingual homepage prototype`。该提交只保存版本，不替换或发布正式主页。
