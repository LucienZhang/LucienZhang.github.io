# A v2 隔离原型验收

2026-09-05。实现任务逐项填写结果与证据，未执行项明确标未测。本设计提交未运行代码验收。

- [ ] 英/中五区块完整，品牌使用现有字体，去编号，区块层级清楚，版权与 Gmail 正确，文章语言标正确。
- [ ] 1440×900、1280×720、768px、390×844、320px 实际浏览器截图；展开菜单/参数/解释/数据表和长文案无横向溢出；200% 放大可用。
- [ ] 年限滑块、加减、金额、利率、重置、数据表真实联动；独立计算测试覆盖零利率、边界、末期清偿、本金合计和非法输入，不用实现自身作为唯一 expected 值来源。
- [ ] AI mock 明确标注，所有状态可审阅，参数修改不自动生成，旧快照与当前结果不混用，引用定位真实指标。
- [ ] 键盘菜单、滑块、表格与解释可操作；可访问名称完整，标题层级、字段 label/error、焦点进入/返回正确，无重复 main、无错误 aria-labelledby、无焦点陷阱。
- [ ] 正文对比度目标 4.5:1，大字及必要控件/图形目标 3:1；焦点清楚、图表不只靠颜色；触控面积与 reduced-motion 实测。
- [ ] SSR 核心内容、图表示例与普通链接完整；关闭 JS 有可读静态结果与说明，控件不伪装可用；hydration 与 SPA 切换无 warning/error 或重复监听。
- [ ] 执行仓库 npm run verify，补覆盖预览路由的浏览器检查；记录环境、命令和结果。无需为静态配色写实现镜像测试。
- [ ] 性能记录生产构建资源：初始必需 JS 去重 gzip ≤160 KiB、公共 CSS ≤36 KiB 为审计候选回归目标；若基线或隔离路由结构导致超出，列清新增长度与共享成本，不掩盖或顺带重构全站。无新增重型图表、字体库或栅格背景，设计 PNG 不进入网站产物。
- [ ] 时间性能固定浏览器/CPU/网络与视口，多次采样，报告统计方法；不把单次 localhost LCP 当线上结论。
- [ ] 正式 /、/zh/ 与文章行为保持现状；没有真实模型、筛股或税务服务，没有全站换色、未授权删除、提交、推送或部署。

交付：可运行预览入口、中英桌面与移动截图、操作说明、验证结果和已知限制，统一在 .ai/phase5 下记录。停止在用户试用审阅，等待正式主页替换授权。
